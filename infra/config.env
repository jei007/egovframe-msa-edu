# infra/scripts(01~09) 공통 설정 — 경로·Docker 이미지 태그(선택).
# localhost port-forward 포트 → infra/lab-access.env
# minikube 옵션(MINIKUBE_DOCKER, MINIKUBE_PROFILE 등) → README 「2. 사전 요구사항」minikube 절

# (Windows 환경에서는 필수) 저장소 루트. 미설정 시 infra/ 기준 상위 디렉터리를 자동 사용합니다.
# EGOVFRAME_MSA_ROOT="/mnt/c/egovframe-msa-edu"

# (선택) 01_build-images.sh / 04_deploy-apps.sh — 미설정 시 your-registry / 1.0.0
# REGISTRY=your-registry
# TAG=1.0.0
